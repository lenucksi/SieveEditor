---
description: Build the SieveEditor project using Maven
---

Build the SieveEditor project by running Maven compilation.

Execute: `mvn clean compile`

If successful, confirm the build completed and show any warnings or errors.
